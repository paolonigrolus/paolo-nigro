#include <stdio.h>

int main()
{
    int n; 
    int z = 0;
    int a;
    int max = 0;
    printf("inserire numero di elementi dell'array\n");
    scanf("%d", &n);
    int array[n];

    while (z<n)
    {
        printf("ïnserire un numero\n");
        scanf("%d", &a);
        array[z] = a; 
        z++; 
    }
    z=0;
    while (z<n)
    {
        if ( )
        
        
    }
}